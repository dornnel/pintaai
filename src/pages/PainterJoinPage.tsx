import { ChatInterface } from '../components/chat/ChatInterface'

// The painter onboarding uses the same conversational agent —
// the agent detects painter intent and adapts the flow.
export function PainterJoinPage() {
  return <ChatInterface />
}
